/**
 * Test case for recordCols.
 * Runs with mocha.
 */
'use strict'

const recordCols = require('../lib/record_cols.js')
const assert = require('assert')
const co = require('co')

describe('record-cols', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Record cols', () => co(function * () {

  }))
})

/* global describe, before, after, it */
