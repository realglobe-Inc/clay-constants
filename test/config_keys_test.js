/**
 * Test case for configKeys.
 * Runs with mocha.
 */
'use strict'

const configKeys = require('../lib/config_keys.js')
const assert = require('assert')
const co = require('co')

describe('config-keys', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Config keys', () => co(function * () {

  }))
})

/* global describe, before, after, it */
