/**
 * Test case for restPaths.
 * Runs with mocha.
 */
'use strict'

const restPaths = require('../lib/rest_paths.js')
const assert = require('assert')
const co = require('co')

describe('rest-paths', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Rest paths', () => co(function * () {

  }))
})

/* global describe, before, after, it */
