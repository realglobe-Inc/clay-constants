/**
 * Test case for sheetNames.
 * Runs with mocha.
 */
'use strict'

const sheetNames = require('../lib/sheet_names.js')
const assert = require('assert')
const co = require('co')

describe('sheet-names', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sheet names', () => co(function * () {

  }))
})

/* global describe, before, after, it */
