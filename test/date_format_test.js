/**
 * Test case for dateFormat.
 * Runs with mocha.
 */
'use strict'

const dateFormat = require('../lib/date_format.js')
const assert = require('assert')

describe('date-format', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Date format', async () => {

  })
})

/* global describe, before, after, it */
