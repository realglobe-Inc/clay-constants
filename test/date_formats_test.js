/**
 * Test case for dateFormats.
 * Runs with mocha.
 */
'use strict'

const dateFormats = require('../lib/date_formats.js')
const assert = require('assert')

describe('date-formats', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Date formats', async () => {

  })
})

/* global describe, before, after, it */
